#!/usr/bin/env bash
#Script that that implements the if elif snd else statement
n=1
while [ $n -le 10 ]
do
	if [ $n -eq 4 ]
	then
	    echo "bad luck"
	elif [ $n -eq 8 ]
	then
	    echo "good luck"
       	else
	    echo "Best School"
	fi
	((n++))
done
